# Core HTTP & Parsing (includes async support)
# aiohttp: Async HTTP client for parallel requests (used in nodes.py, validate.py, fallback_checks.py)
# asyncio: Built into Python 3.12+ (no install needed)
aiohttp>=3.9.0,<4.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0
tldextract>=5.0.0

# LangGraph + LangChain (async-enabled: ainvoke, astream)
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
openai>=1.0.0
pydantic>=2.5.0,<3.0 

# Crawl4AI (JS rendering for dynamic content)
crawl4ai>=0.8.0

# ML / NLP
scikit-learn>=1.4.0,<2.0
faiss-cpu>=1.7.4

# Excel & Data
openpyxl>=3.1.0
pandas>=2.1.0,<3.0

# Streamlit
streamlit>=1.35.0

# Environment & Utils
python-dotenv>=1.0.0
colorama>=0.4.6
PyYAML>=6.0
